/** @type {import('prettier').Config} */
const config = {
    tabWidth: 4,
    useTabs: false,
    trailingComma: "all",
    singleQuote: false,
    semi: true,
    printWidth: 100,
};

export default config;
